diff --git a/dist/client/index.d.ts b/dist/client/index.d.ts
index 57f8ffcb729c5d3e3f087ac136635b584e34bb8e..487ee806cd69e5366a135b07e804c1ce7c891623 100644
--- a/dist/client/index.d.ts
+++ b/dist/client/index.d.ts
@@ -10,6 +10,7 @@ export type R2ConfigInput = {
     accessKeyId?: string;
     secretAccessKey?: string;
     bucketName?: string;
+    jurisdiction?: string;
 };
 export interface RegisterRoutesOptions {
     /** Prefix for HTTP routes, defaults to "/files". */
diff --git a/dist/client/index.js b/dist/client/index.js
index 939193a067f970c7abd6d491a20cc0253b730f33..e6b29c9424cffd1a5e04e2b9ca0558fc3167db0d 100644
--- a/dist/client/index.js
+++ b/dist/client/index.js
@@ -10,6 +10,7 @@ const R2_ENV_VARS = {
     accessKeyId: "R2_ACCESS_KEY_ID",
     secretAccessKey: "R2_SECRET_ACCESS_KEY",
     bucketName: "R2_BUCKET_NAME",
+    jurisdiction: "R2_JURISDICTION",
 };
 const readEnv = (key) => typeof process !== "undefined" ? process.env[key] : undefined;
 const resolveR2Config = (input) => {
@@ -18,8 +19,14 @@ const resolveR2Config = (input) => {
         accessKeyId: input?.accessKeyId ?? readEnv(R2_ENV_VARS.accessKeyId),
         secretAccessKey: input?.secretAccessKey ?? readEnv(R2_ENV_VARS.secretAccessKey),
         bucketName: input?.bucketName ?? readEnv(R2_ENV_VARS.bucketName),
+        jurisdiction: input?.jurisdiction ?? readEnv(R2_ENV_VARS.jurisdiction),
     };
-    const hasAll = Object.values(config).every((value) => Boolean(value));
+    const hasAll = [
+        config.accountId,
+        config.accessKeyId,
+        config.secretAccessKey,
+        config.bucketName,
+    ].every((value) => Boolean(value));
     if (!hasAll) {
         return null;
     }
diff --git a/dist/component/r2.d.ts b/dist/component/r2.d.ts
index ee11e037994d3727606112763e8c353b01ea4c6a..33ef8d9d5e6d542866dea563ced0c3fe15d69f0d 100644
--- a/dist/component/r2.d.ts
+++ b/dist/component/r2.d.ts
@@ -5,12 +5,14 @@ export declare const r2ConfigValidator: import("convex/values").VObject<{
     accessKeyId: string;
     secretAccessKey: string;
     bucketName: string;
+    jurisdiction?: string | undefined;
 }, {
     accountId: import("convex/values").VString<string, "required">;
     accessKeyId: import("convex/values").VString<string, "required">;
     secretAccessKey: import("convex/values").VString<string, "required">;
     bucketName: import("convex/values").VString<string, "required">;
-}, "required", "accountId" | "accessKeyId" | "secretAccessKey" | "bucketName">;
+    jurisdiction: import("convex/values").VOptional<import("convex/values").VString<string, "required">, "optional">;
+}, "required", "accountId" | "accessKeyId" | "secretAccessKey" | "bucketName" | "jurisdiction">;
 export declare function requireR2Config(config: R2Config | undefined | null, context?: string): R2Config;
 export declare function createR2Client(config: R2Config): S3Client;
 export declare function getR2UploadUrl(config: R2Config, key: string): Promise<string>;
diff --git a/dist/component/r2.js b/dist/component/r2.js
index 921287814d5330f35d4674b595ac3cf8585bc3c2..bc3e9ca5dcdf5315c15f2f83ee8b89d88f1be7fc 100644
--- a/dist/component/r2.js
+++ b/dist/component/r2.js
@@ -7,6 +7,7 @@ export const r2ConfigValidator = v.object({
     accessKeyId: v.string(),
     secretAccessKey: v.string(),
     bucketName: v.string(),
+    jurisdiction: v.optional(v.string()),
 });
 export function requireR2Config(config, context) {
     if (config) {
@@ -18,7 +19,8 @@ export function requireR2Config(config, context) {
 export function createR2Client(config) {
     return new S3Client({
         region: "auto",
-        endpoint: r2EndpointFromAccountId(config.accountId),
+        endpoint: r2EndpointFromAccountId(config.accountId, config.jurisdiction),
+        forcePathStyle: Boolean(config.jurisdiction),
         credentials: {
             accessKeyId: config.accessKeyId,
             secretAccessKey: config.secretAccessKey,
diff --git a/dist/shared/r2.d.ts b/dist/shared/r2.d.ts
index df393ede04c6ef88d282e4a05802e6d42154ed6b..44718e0e375a80ea3902d6766adf7138fc0763ac 100644
--- a/dist/shared/r2.d.ts
+++ b/dist/shared/r2.d.ts
@@ -1,2 +1,2 @@
-export declare function r2EndpointFromAccountId(accountId: string): string;
+export declare function r2EndpointFromAccountId(accountId: string, jurisdiction?: string): string;
 //# sourceMappingURL=r2.d.ts.map
diff --git a/dist/shared/r2.js b/dist/shared/r2.js
index bac6354fab4702e3f38256ae62b8239c4d1ca4de..e6d8a1766e2bcf9393ec64684879d13697fd96d4 100644
--- a/dist/shared/r2.js
+++ b/dist/shared/r2.js
@@ -1,4 +1,8 @@
-export function r2EndpointFromAccountId(accountId) {
-    return `https://${accountId}.r2.cloudflarestorage.com`;
+export function r2EndpointFromAccountId(accountId, jurisdiction) {
+    const normalizedJurisdiction = jurisdiction?.trim().toLowerCase();
+    const jurisdictionSegment = normalizedJurisdiction
+        ? `.${normalizedJurisdiction}`
+        : "";
+    return `https://${accountId}${jurisdictionSegment}.r2.cloudflarestorage.com`;
 }
 //# sourceMappingURL=r2.js.map
diff --git a/src/client/index.ts b/src/client/index.ts
index 88b1377e9121cdbddb9cdc1282d88f3fce2962b4..65e188ba6216080a1b62ef660e485cb572ce1421 100644
--- a/src/client/index.ts
+++ b/src/client/index.ts
@@ -55,6 +55,7 @@ export type R2ConfigInput = {
   accessKeyId?: string;
   secretAccessKey?: string;
   bucketName?: string;
+  jurisdiction?: string;
 };
 
 const R2_ENV_VARS: Record<keyof R2Config, string> = {
@@ -62,6 +63,7 @@ const R2_ENV_VARS: Record<keyof R2Config, string> = {
   accessKeyId: "R2_ACCESS_KEY_ID",
   secretAccessKey: "R2_SECRET_ACCESS_KEY",
   bucketName: "R2_BUCKET_NAME",
+  jurisdiction: "R2_JURISDICTION",
 };
 
 const readEnv = (key: string) =>
@@ -74,9 +76,15 @@ const resolveR2Config = (input?: R2ConfigInput): R2Config | null => {
     secretAccessKey:
       input?.secretAccessKey ?? readEnv(R2_ENV_VARS.secretAccessKey),
     bucketName: input?.bucketName ?? readEnv(R2_ENV_VARS.bucketName),
+    jurisdiction: input?.jurisdiction ?? readEnv(R2_ENV_VARS.jurisdiction),
   };
 
-  const hasAll = Object.values(config).every((value) => Boolean(value));
+  const hasAll = [
+    config.accountId,
+    config.accessKeyId,
+    config.secretAccessKey,
+    config.bucketName,
+  ].every((value) => Boolean(value));
   if (!hasAll) {
     return null;
   }
diff --git a/src/component/r2.ts b/src/component/r2.ts
index a38216cdd9187bef126cbc4a829d8d8700774ae0..d0abcc54f9f8c06405b9f550164208db9f263b4d 100644
--- a/src/component/r2.ts
+++ b/src/component/r2.ts
@@ -14,6 +14,7 @@ export const r2ConfigValidator = v.object({
   accessKeyId: v.string(),
   secretAccessKey: v.string(),
   bucketName: v.string(),
+  jurisdiction: v.optional(v.string()),
 });
 
 export function requireR2Config(
@@ -30,7 +31,11 @@ export function requireR2Config(
 export function createR2Client(config: R2Config) {
   return new S3Client({
     region: "auto",
-    endpoint: r2EndpointFromAccountId(config.accountId),
+    endpoint: r2EndpointFromAccountId(
+      config.accountId,
+      config.jurisdiction,
+    ),
+    forcePathStyle: Boolean(config.jurisdiction),
     credentials: {
       accessKeyId: config.accessKeyId,
       secretAccessKey: config.secretAccessKey,
diff --git a/src/shared/r2.ts b/src/shared/r2.ts
index 2837a83d82139c692fd08d690ef09f10b1964f3b..f9f075de403d950e8b5b6d534ba0677c20a0bb69 100644
--- a/src/shared/r2.ts
+++ b/src/shared/r2.ts
@@ -1,3 +1,10 @@
-export function r2EndpointFromAccountId(accountId: string) {
-  return `https://${accountId}.r2.cloudflarestorage.com`;
+export function r2EndpointFromAccountId(
+  accountId: string,
+  jurisdiction?: string,
+) {
+  const normalizedJurisdiction = jurisdiction?.trim().toLowerCase();
+  const jurisdictionSegment = normalizedJurisdiction
+    ? `.${normalizedJurisdiction}`
+    : "";
+  return `https://${accountId}${jurisdictionSegment}.r2.cloudflarestorage.com`;
 }
