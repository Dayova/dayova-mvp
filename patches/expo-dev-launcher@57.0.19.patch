diff --git a/plugin/build/withDevLauncher.js b/plugin/build/withDevLauncher.js
index 6eb2d79..6bda947 100644
--- a/plugin/build/withDevLauncher.js
+++ b/plugin/build/withDevLauncher.js
@@ -31,5 +31,5 @@ const withStripLocalNetworkKeysForRelease = (config) => {
             return config;
         }
-        project.addBuildPhase([], 'PBXShellScriptBuildPhase', buildPhaseName, nativeTargetId, {
+        const addedPhase = project.addBuildPhase([], 'PBXShellScriptBuildPhase', buildPhaseName, nativeTargetId, {
             shellPath: '/bin/sh',
             shellScript: `# Strip dev-launcher-specific local network permission keys from non-Debug builds
@@ -67,4 +67,5 @@ fi
 `,
         });
+        addedPhase.buildPhase.alwaysOutOfDate = 1;
         const targetPhases = project.pbxNativeTargetSection()[nativeTargetId]?.buildPhases ?? [];
         const addedIdx = targetPhases.findIndex((p) => p.comment === buildPhaseName);
diff --git a/plugin/src/withDevLauncher.ts b/plugin/src/withDevLauncher.ts
index f95dc7d..363fb01 100644
--- a/plugin/src/withDevLauncher.ts
+++ b/plugin/src/withDevLauncher.ts
@@ -45,5 +45,5 @@ const withStripLocalNetworkKeysForRelease: ConfigPlugin = (config) => {
     }
 
-    project.addBuildPhase([], 'PBXShellScriptBuildPhase', buildPhaseName, nativeTargetId, {
+    const addedPhase = project.addBuildPhase([], 'PBXShellScriptBuildPhase', buildPhaseName, nativeTargetId, {
       shellPath: '/bin/sh',
       shellScript: `# Strip dev-launcher-specific local network permission keys from non-Debug builds
@@ -82,5 +82,6 @@ fi
 `,
     });
+    addedPhase.buildPhase.alwaysOutOfDate = 1;
 
     const targetPhases: { value: string; comment?: string }[] =
       project.pbxNativeTargetSection()[nativeTargetId]?.buildPhases ?? [];
